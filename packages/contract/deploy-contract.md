- npx hardhat compile (This will compile the smart contract, generate new ABI and make it ready for deployment)

- pnpm deploy (This will run deployment script, update deployments.json, which contains address of deployed contracts)
